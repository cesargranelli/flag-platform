{
  "name": "flag-platform-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "Suíte E2E (Playwright) do Flag Platform — staging efêmero (issue #202). Node.js independente; fora do workspace Flutter/melos.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "install:browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0"
  }
}
